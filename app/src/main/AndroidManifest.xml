<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.thyroid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Thyroid">
        <activity android:name=".HistoricalReportPage"></activity>
        <activity android:name=".ViewReportPage" />
        <activity android:name=".FunctionalPage_Patient" />
        <activity android:name=".FunctionalPage_Doctor" />
        <activity android:name=".EditReportPage" />
        <activity android:name=".ReportPage" />
        <activity android:name=".PersonalPageActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>